# Grafana のメール通知（GF_SMTP_*）用。実値を入れたら
# bootstrap/kubeseal.sh で SealedSecret 化してからコミットすること。
apiVersion: v1
kind: Secret
metadata:
  name: grafana-smtp
  namespace: watch
type: Opaque
stringData:
  GF_SMTP_HOST: ""
  GF_SMTP_FROM_ADDRESS: ""
  GF_SMTP_USER: ""
  GF_SMTP_PASSWORD: ""
